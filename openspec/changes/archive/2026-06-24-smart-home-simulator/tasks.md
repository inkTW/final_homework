## 1. 專案基礎與基底類別設計

- [x] 1.1 建立 C++ 專案專屬目錄結構與進入點 `main.cpp`
- [x] 1.2 宣告並實作智慧設備 `SmartDevice` 基底類別（包含虛擬解構子、狀態與虛擬介面）
- [x] 1.3 宣告並實作自動化規則 `AutomationRule` 基底類別（包含虛擬解構子與 checkAndTrigger 介面）

## 2. 智慧設備與自動化規則衍生類別實作

- [x] 2.1 實作 `SmartLight` 衍生類別（包含亮度、色彩屬性與多型覆寫）
- [x] 2.2 實作 `SmartThermostat` 衍生類別（包含目標溫度、模式屬性與多型覆寫）
- [x] 2.3 實作 `SmartCamera` 衍生類別（包含錄影狀態、動態感應屬性與多型覆寫）
- [x] 2.4 實作 `TemperatureRule` 衍生類別（檢查環境溫度並觸發特定設備狀態）
- [x] 2.5 實作 `TimeRule` 衍生類別（檢查環境時間並觸發特定設備狀態）

## 3. 控制中心與自動化引擎實作

- [x] 3.1 實作 `SmartHome` 控制中心類別，使用 `std::vector<std::unique_ptr<SmartDevice>>` 與 `std::vector<std::unique_ptr<AutomationRule>>` 容器
- [x] 3.2 實作自動化引擎輪詢邏輯 `runAutomation()`，定期檢查所有規則並觸發動作
- [x] 3.3 實作系統事件日誌（Event Logger）功能，以 STL 容器快取系統操作日誌

## 4. 檔案持久化讀寫模組

- [x] 4.1 實作設定檔載入模組，讀取 `config.txt` 動態建立並還原設備與規則物件
- [x] 4.2 實作設定檔儲存模組，將目前的設備與規則配置序列化並寫入 `config.txt`
- [x] 4.3 實作日誌導出模組，將快取的事件日誌以 CSV 格式寫出至 `system_log.csv`

## 5. 終端機模擬 UI 與環境模擬

- [x] 5.1 實作主選單、儀表板顯示面板，能格式化輸出當前環境與各設備狀態
- [x] 5.2 實作環境狀態模擬功能（調整時間、室溫、動態感測觸發）
- [x] 5.3 實作終端機輸入防呆防崩潰機制，確保輸入無效字串時可自動清除異常狀態並提示重新輸入
- [x] 5.4 撰寫完整專案測試檔案、範例 `config.txt`，並進行編譯與運作整合測試
